#include <bits/stdc++.h>
using namespace std;

int main() {
	int a, b; cin >> a >> b;
	int counter = 0;
	while(b != a){
		if(b > a && b%2 == 0){
			b = b/2;
		}
		else{
			b += 1;
		}
		counter ++;
	}
	cout << counter;
}
